import React from "react";

type Props = {};

const TeachingCompetency = (props: Props) => {
  return (
    <section className="flex flex-col items-center justify-center h-screen">
      TeachingCompetency
    </section>
  );
};

export default TeachingCompetency;
